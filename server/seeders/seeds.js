const faker = require('faker');

const db = require('../config/connection');
// this line should require the models

db.once('open', async () => {
    // create Teachers

    // create students

    // create parents

    // create messages
})